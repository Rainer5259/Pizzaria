import * as fontStyleSetted from './fontStyles'
import * as colorsPallet from './colors'

export const fontStyles = {
  ...fontStyleSetted.default
}
export const colors = {
  ...colorsPallet.default
}
